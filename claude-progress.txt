---
Session: 2026-08-19 (Session 7 — paged KV cache F034, HTTP serving F035)
Completed: F034 + F035 (34/36 features passing; 16/16 ctest green + 6/6 pytest)
Notes:
  - F034 PAGED KV CACHE (src/model/paged_kv.{hpp,cpp}): the vLLM
    PagedAttention memory model on the CPU — 16-token pages (vLLM's
    default block_size), PagePool owning physical pages + a LIFO free
    list SHARED across sequences, PagedKvCache = one sequence's block
    table (logical block idx -> page id). One page = one heap buffer
    holding all 24 layers' K+V rows for its 16 positions (stable
    pointers; block id shared across layers, same as vLLM). The header
    comment of paged_kv.hpp is a deliberate design note mapping every
    piece onto vLLM vocabulary name-for-name AND listing what was
    deliberately simplified away (no prefix sharing/CoW/refcounts, no
    eviction policy, no preemption/swap, eager backing) — written to be
    quotable in kvcached conversations.
  - Dispatch design: layer_forward is now template<class Cache> with
    explicit instantiations for KvCache and PagedKvCache in qwen2.cpp —
    NOT virtual, because k_row/v_row sit in the attention inner loop.
    Engine::forward does one if/else per call into a templated
    forward_with; Engine holds unique_ptrs (exactly one layout live) and
    grew a KvLayout ctor param (default contiguous — no caller broke).
    cache.prepare(pos0, tokens) is the only new hook (no-op contiguous,
    page allocation paged); Engine::reset() releases pages to the pool.
  - F034 VERIFICATION (test_kvcache parts C/D/E): paged logits
    BIT-IDENTICAL to contiguous at prefill + all 40 greedy steps
    (crossing >= 2 page boundaries; paging relocates rows, never changes
    values or combine order, so exactness is the design's proof, same
    argument as F020 cached-vs-recompute). Memory: 81-token generation
    @ max_seq 2048 = 2.25 MiB paged high-water vs 48.0 MiB contiguous
    preallocation (21x, test prints both). Perf: 21.76 ms/token
    contiguous vs 21.88 paged = 0.6% decode overhead on this run (the
    block-table div/mod indirection; loose test bound, real number in
    the print).
  - THE CHURN TEST (test_paged_alloc, model-free so CI always runs it):
    written against the exact failure shape of kvcached #437 — an
    allocator handing out page ids beyond backed capacity. alloc()
    backs memory BEFORE returning an id, so the state is unrepresentable
    here by construction, and the test asserts the invariant on every
    alloc anyway: 2000 random grow/reset steps across 3 sequences
    sharing a 6-page pool (3 full sequences would need 12 — permanent
    pressure), 329 pool exhaustions recovered by evict-and-continue,
    pattern re-verification of EVERY live row after EVERY step (catches
    aliasing/stale block tables/double allocation), pool accounting
    (live == sum of pages_held) checked each step, LIFO reuse and
    zero pool growth after free/realloc asserted. 9637 checks.
  - F035 HTTP SERVING (src/server/http.* + serve.*): `nanoserve serve
    <model_dir> [--port P] [--int8] [--threads N]`. HTTP/1.1 on raw
    POSIX sockets, blocking, one connection at a time, Connection:
    close everywhere — documented as the honest shape of a
    single-sequence engine, not a shortcut (F036 is what changes it).
    POST /v1/completions, classic OpenAI schema subset: prompt (RAW
    completion, no chat template — that's the endpoint's contract; the
    CLI stays the chat interface), max_tokens, temperature/top_k/top_p/
    seed, n=1 only, stream. Non-stream: text_completion object w/ one
    choice, finish_reason stop|length, usage counts (completion_tokens
    counts the sampled stop token, text never includes it). Stream: SSE,
    one chunk per StreamDecoder piece (UTF-8-safe) emitted pre-next-
    forward, then an empty finish_reason chunk, then [DONE]. Errors are
    OpenAI-shaped {"error":{...}}: 400 bad JSON/params, 404, 405, 413,
    431. Server engine runs KvLayout::paged. json::quote() added to
    core/json for serialization (control-char + UTF-8 tested, parse-
    inverted in test_json).
  - F035 VERIFICATION: test_server (ctest) forks/execs the REAL binary
    ($<TARGET_FILE:nanoserve> passed as argv[1]) with --port 0, parses
    the "listening on http://127.0.0.1:<port>" readiness line (printed
    only after model load), and talks to it through raw client sockets:
    41 checks — schema, usage.prompt_tokens equal to a locally loaded
    tokenizer's count, greedy determinism across two requests, SSE
    chunks reassembling byte-identically to the non-streamed text with
    finish_reason only on the final empty chunk, and the 400/405/404
    paths. Manual curl on port 8080 verified both modes by hand
    ("The capital of France is" -> " Paris. It is the largest city in").
  - GOTCHA: an early test_json edit left literal 0x01/0x1f control
    bytes inside a C++ raw string (invisible in the terminal, wrong
    expectation); rewrote the check with explicit escapes. If a raw
    string check ever looks "obviously right" but fails, hexdump it.
  - ctest 33.8s -> 42.0s (test_kvcache now loads a second, paged engine
    and re-times decode; test_server loads one more).
Next: F036 continuous batching, the last real feature (F004 CI still
  waits on the user creating the GitHub remote). Sketch from this
  session's vantage point: (1) engine side — a batched decode step
  needs per-row (cache, pos) instead of one sequence's (pos0+t): stack
  each live sequence's current token into [n_seqs, hidden] so the qkv/
  o/mlp linears batch through the existing GEMM path (that's where the
  throughput comes from), keep rope+attention per row against that
  row's own PagedKvCache — PagePool was built shared for exactly this;
  (2) server side — request queue + one worker thread owning the
  engine; admit new requests between decode steps (prefill can stay
  whole-prompt, one sequence at a time, to keep step latency bounded);
  per-request Sampler and StreamDecoder already have no shared state;
  (3) verification per feature list — 4 concurrent requests all
  correct, combined throughput > serial (print both), single-request
  latency regression < 20%. Watch decode determinism: batched GEMM row
  results must be identical to the 1-row GEMV path or greedy outputs
  shift — test_ops already covers linear tokens>1 vs tokens=1 parity,
  lean on it. Also consider capping n_seqs by PagePool max_pages and
  returning 429 or queueing when full — the pool exhaustion path is
  already loud and tested.
---
Session: 2026-08-19 (Session 6 — Metal backend, F033)
Completed: F033 (32/36 features passing; 14/14 ctest green + 6/6 pytest)
Notes:
  - THE HEADLINE (a negative result, published as such): at 0.5B the GPU
    LOSES decode — fp32 metal 30.9 +/- 0.9 tok/s vs 50.8 +/- 3.2 CPU
    (1.64x slower), int8 metal 38.2 +/- 0.2 vs 147.0 +/- 0.4 (3.85x
    slower). Cause is architectural, not a kernel bug: only the linears
    are offloaded and the CPU consumes each result immediately (RoPE,
    attention, silu), so every projection is a synchronous encode/commit/
    wait round-trip — ~121 per decode token (5 GEMVs x 24 layers +
    lm_head; k/v sit below the dispatch floor). int8-metal decode at
    26.2 ms/token vs fp32-metal 32.4 ms is a consistency check: the
    6.2 ms delta ~= the ~1.4 GB/token weight-traffic difference. CPU
    stays the default; --backend metal is opt-in. Prefill: fp32 metal at
    parity with fp32 CPU (144.2 vs 144.5 tok/s), int8 metal 3.6x SLOWER
    than the sdot CPU prefill — the naive simdgroup-per-column kernel
    (even with 8-token tiling) is nowhere near Accelerate-class GEMM.
    Beating 0.5B CPU decode on GPU would need fused/graph dispatch
    (megakernels or at least whole-layer command buffers), i.e.
    restructuring the engine — out of F033's scope, noted honestly.
  - WHAT LANDED: src/core/metal.{hpp,mm} (ObjC++, runtime-compiled MSL
    embedded as a string; stub .cpp elsewhere so x86 CI links). Kernels:
    one simdgroup per output column, lanes stride d_in (coalesced), 8-token
    tiles so a weight row is read once per tile; linear_q8 variant
    dequantizes int8 weights in-register against FP32 activations
    (activation quantization is a CPU-sdot trick; on GPU the win is weight
    bytes, so skipping it is free accuracy). ops::set_backend(cpu|metal)
    routes linear/linear_q8 above kMetalMinMacs (= 2^19, same floor as the
    thread pool; k/v GEMVs stay CPU). Weight residency: Engine ctor
    registers every weight buffer iff metal is selected — page-aligned
    large allocations get ZERO-COPY newBufferWithBytesNoCopy wraps
    (unified memory; no second copy of the model), small buffers are
    copied once; dtor unregisters before free, unregistered pointers work
    via per-call staging (what the parity test uses). CLI --backend,
    Python set_backend()/metal_available(), bench rows carry a backend
    field (report.py keys on engine/precision/backend, old files default
    cpu).
  - ACCURACY: test_metal (skips 77 without a GPU): fp32 parity worst
    2.9e-5 combined abs/rel across GEMV/GEMM/ragged/sub-simdgroup shapes
    (bound 1e-3); q8-vs-dequantized-reference worst 7.2e-6; routing floor
    checks bit-exact both sides. On the real model: fp32-metal logits
    within 2.2e-5 of CPU, greedy IDENTICAL to HF goldens 4/4; int8-metal
    greedy BIT-IDENTICAL to the fp32 goldens 4/4 — strictly better than
    the CPU sdot path (haiku near-tie flip at step 18 doesn't happen
    without activation quantization), guarded by test_quant's margin frame
    if it ever diverges. CLI cross-check: cpu vs metal ids identical
    (fp32 haiku 21 tokens, int8 KV prompt 32 tokens).
  - Bench protocol: 2 full runs (first had int8-cpu prefill flagged
    HIGH-VARIANCE 502.8 +/- 95.4 from session background load; idle re-run
    546.5 +/- 2.2 matches session 5's 545.8 — both files kept, newest
    wins). Metal rows reproduced across both runs (30.9/31.6, 38.2/38.6).
    Numbers recorded at clean commit cea0581.
  - GOTCHAS for future GPU work: ARC needs `id<MTLBuffer> __strong&` for
    reference params; pointer-keyed residency is only safe because
    register/unregister bracket the vectors' lifetime (Engine ctor/dtor) —
    NEVER auto-register inside the linear call (freed+reused addresses
    would serve stale weights silently). ctest 20.4 -> 33.8s (test_metal
    loads fp32+int8 engines and re-runs goldens under 2 backends).
Next: remaining CPU perf threads: prefill GEMM blocking (llama.cpp f32
  prefill lead is 3.9x, mostly Accelerate BLAS) and 4-row-blocked q8 GEMV
  (last 1.33x decode gap). Or F034 paged KV cache (next unfinished feature
  in list order). F004 (CI) still waits on the user creating the GitHub
  remote. If GPU decode is ever revisited: batch whole layers into one
  command buffer first, measure dispatch overhead directly with a
  timestamp-sampled empty kernel.
---
Session: 2026-08-19 (Session 5 — integer sdot int8 kernel)
Completed: no feature-list flips (perf work inside F027's scope); still 31/36,
  13/13 ctest green + 6/6 pytest
Notes:
  - THE HEADLINE: int8 decode 106.2 -> 145.6 +/- 4.4 tok/s (bench harness,
    5 threads, medians consistent across 3 runs: 145.0/146.0/145.6); the
    q8 decode gap vs llama.cpp Q8_0 closed 1.86x -> 1.34x (195.6 +/- 7.3
    re-measured same session). Prefill was the bigger surprise: 184.9 ->
    545.8 tok/s (2.95x) because sdot removes the instruction-throughput
    bottleneck that the compute-bound phase feels hardest; int8 TTFT is now
    75 ms, 2.6x faster than our OWN fp32 prefill. README regenerated by
    report.py from the new results JSON; the misattributed first bench run
    (dirty tree recorded as e178963) was deleted and re-run on the real
    commit eca33ea.
  - Kernel (ops.cpp dot_q8_blocks + linear_q8): activations quantized to
    int8 at runtime per 32-VALUE BLOCK (quantize_row_q8_blocks; same shared
    quantizer as the offline weights), each block's dot exact in int32 via
    vdotq_s32 (2 sdot + 1 cvt + 1 fma per 32 weights — llama.cpp Q8_0
    structure), per-block activation scale applied by fma, per-row weight
    scale once at the end. Weights stay per-row/8.0 BPW — file format
    unchanged, no requantize needed. Scalar fallback computes the same
    per-block int32 sums (x86 CI correctness); gated on
    __ARM_FEATURE_DOTPROD (baseline on all Apple silicon). Overflow-safe by
    a mile: 32 * 127^2 ~= 5e5 << 2^31 per block sum.
  - ACCURACY STORY (the honest part): per-token-ROW activation scales were
    tried FIRST and rejected by the goldens — activation outliers compress
    a row-wide scale enough that the haiku prompt's FIRST greedy token
    flipped (its fp32 top-2 margin is only 0.007). Per-32-block passes
    top-1 on all 3 logits goldens and keeps 3/4 greedy continuations
    BIT-identical; the haiku diverges at step 18/21 where the fp32 top-2
    margin is 0.031 — a genuine near-tie (the top-10 logit noise vs HF is
    0.17-0.36 now, was 0.12-0.27 with the widening kernel; margins probe:
    the 4 goldens contain exactly two sub-0.1-margin steps, 0.003 and
    0.031 — int8 kept one by luck, flipped the other; EVERY token with
    margin >= 0.12 is preserved). Bit-identical is not achievable for ANY
    int8-activation scheme on a 0.03-margin token; llama.cpp Q8_0 carries
    the same class of noise. test_quant now ENFORCES this frame: on any
    divergence it teacher-forces the golden prefix through a fp32 engine
    and fails if the flipped step's margin was >= 0.1 (a decisive-token
    flip = regression, a near-tie flip = documented physics).
  - test_ops gained a linear_q8 math-replay check so test_ops_scalar
    actually executes the scalar integer fallback; test_alloc gained an
    int8-engine section (the new activation scratch in ops.cpp is grow-only,
    prefill-sized: 16 int8 decode steps = 0 allocations). Cross-build note:
    per-block int sums are bit-equal NEON vs scalar, but the fp32 summation
    ACROSS blocks reorders, so cross-build parity is ~1e-5 like the fp32
    kernels (within one build: bit-identical at any thread count, memcmp'd).
  - ctest 16.2s -> ~20.5s (test_alloc +int8 engine load, test_quant +fp32
    engine for the margin check) — acceptable, documented here.
Next: F033 Metal backend (matmul parity test + --backend metal + bench) is
  the next unfinished feature; expect a full session (Metal build wiring,
  parity tolerance decisions, bench row). Remaining perf story if preferred:
  prefill GEMM blocking (llama.cpp's remaining 2.7x prefill lead is largely
  Accelerate BLAS GEMM) and the last 1.34x q8 decode gap (llama.cpp does
  4-row-blocked GEMV with better weight-cache reuse). F004 (CI) still waits
  on the user creating the GitHub remote.
---
Session: 2026-08-19 (Session 4 — int8, Python bindings, and the benchmark)
Completed: F027-F032 (31/36 features passing; 13/13 ctest green + 6/6 pytest)
Notes:
  - F027 int8 (core/quant.*, ops::linear_q8): symmetric per-row absmax
    (scale = absmax/127), offline `nanoserve quantize` writes a safetensors
    file (I8 tensors keep their names + "<name>.scale" F32 companions;
    473 MiB vs 942 MiB bf16). One Weight struct dispatches fp32/int8 through
    the same load + forward path; embedding rows dequantize on lookup. NEON
    kernel widens int8->fp32 in-register (same 4-accumulator shape as
    dot_f32) so the math equals dequantize-then-dot exactly; only the memory
    traffic changes. CLI: quantize + generate --int8.
  - F027 quality, measured: greedy continuations IDENTICAL to the fp32
    goldens on ALL 4 prompts (8/8, 21/21, 8/8, 32/32 tokens); top-1
    unchanged on all 3 logits goldens; top-10 logit shift vs HF fp32 max
    0.12-0.27 (logits ~20); max dequant err respects scale/2. SUBTLETY: on
    real weights a value sitting EXACTLY on a .5 tie can round one
    product-ulp past scale/2 because the quantizer multiplies by
    (127/absmax), not divides by scale — observed rel 1.000007, test bound
    is scale/2 * 1.0001 with a comment.
  - F027 HYPOTHESIS RESULT (session 3 predicted int8 helps decode more than
    flop math suggests): CONFIRMED. Same fma count + EXTRA widening work,
    yet decode went 22.3 -> 11.75 ms/token (1.9x, CLI --stats medians of 3
    interleaved runs); prefill only 1.35x (compute-bound, as expected).
    Not the full 4x bandwidth ratio: the widening adds compute and non-weight
    traffic remains; int8 runs were also visibly steadier than fp32.
  - F028/F029 (src/python/bindings.cpp, python/nanoserve/, pyproject.toml):
    scikit-build-core + pybind11; pip install -e . AND pip wheel . (full
    build isolation) both verified. GenerateResult(text, token_ids,
    prompt_tokens, ttft_ms, tokens_per_second). Streaming = TokenStream
    iterator, ONE decode step per __next__ (no threads/queues; the Python
    for-loop is the decode loop), GIL released around every forward, UTF-8
    holdback chunks never yielded empty. pybind11 module compiles WITHOUT
    nanoserve_warnings (its headers aren't -Wconversion-clean); core still
    warnings-as-errors. pytest 6/6: ids+text match the CLI exactly, seeded
    determinism, per-chunk timestamps prove incremental arrival,
    stream == non-stream, stats plausible.
  - F030-F032 (bench/run.py, setup_llamacpp.sh, report.py): pinned workload
    (41-token chat prompt x 64 greedy tokens), warmup 1, repeats 5,
    mean+/-std, >10% dispersion flagged, machine info + engine versions in
    JSON. llama.cpp pinned via bench/llamacpp.lock (5112b97), built CPU-only
    (Metal OFF), f32 GGUF converted FROM OUR safetensors so fp32 is exactly
    like-for-like; Q8_0 quantized from that (per-32-block, 8.5 BPW vs our
    per-row 8.0 — documented, not hidden). report.py rewrites README between
    BENCH markers; table/gaps/hardware all injected from results JSON.
  - BENCH RESULTS (M3 Pro, 5 threads, in the README now): nanoserve fp32
    51.3+/-1.0 decode tok/s vs llama.cpp f32 55.4+/-3.0 (1.08x gap — near
    parity); int8 106.2+/-0.7 vs Q8_0 197.8+/-1.2 (1.86x gap); prefill
    140.8/184.9 vs 552/1514 (llama.cpp's lead there is largely Accelerate
    BLAS GEMM + real int8 dot kernels). THREADS MATTER: at 11 threads
    llama.cpp decode DROPS to 76 tok/s q8_0 (E-core contention) and ours to
    82.9; decode is fastest at 5 P-core threads for BOTH engines, so the
    table uses 5 and says why. The 1.86x q8 gap has a clear next step:
    llama.cpp computes int8 dot products in integer SIMD (sdot) while we
    widen to fp32 — that plus prefill GEMM blocking is the remaining perf
    story.
  - Gotchas hit: convert_hf_to_gguf.py globs EVERY *.safetensors in the
    model dir (choked on our model.int8.safetensors -> convert from a clean
    symlink dir); it also imports sentencepiece just to probe before falling
    back to BPE (pip install sentencepiece); llama.cpp's llama-cli target
    doesn't exist in a tools-only build — llama-bench + llama-quantize are
    all the harness needs.
Next: stretch features in order — F033 Metal backend (matmul parity test +
  --backend metal + bench), F034 paged KV cache, F035 HTTP serve, F036
  continuous batching. Also worth considering (not on the list): integer
  sdot int8 kernel to close the 1.86x decode gap, and prefill GEMM blocking
  — each would need a measured before/after in bench/results. F004 (CI on
  GitHub) still waits on the user creating the remote repo.
---
Session: 2026-08-19 (Session 3 — Sampling, streaming, and the perf arc)
Completed: F022-F026 (25/36 features passing; 12/12 ctest green)
Notes:
  - F022 sampling (src/model/sampler.*): temperature -> top-k -> top-p in HF's
    warper order, then renormalize and draw. Verified two ways: 9 golden
    distributions from transformers' Temperature/TopK/TopP warpers
    (gen_golden.py::gen_sampling_golden, tests/data/sampling_golden.json —
    candidate SET must match exactly, probs to 1e-6), plus hand-built
    distributions for the edges. PROBED HF FIRST to pin the top-p rule: a
    token is kept while the cumulative probability BEFORE it is < top_p, so
    the crossing token is included (TopPLogitsWarper with p=[.5,.3,.2] and
    top_p=0.5 keeps exactly one token). Golden cases deliberately avoid ties
    and exact cumsum boundaries — there HF depends on torch's unstable sort,
    so a golden would be testing noise; our own tie rule (lower id wins) is
    pinned by a separate hand-built check. Determinism: mt19937_64 + a
    hand-rolled uniform from the top 53 bits (NOT
    uniform_real_distribution, whose mapping is implementation-defined).
    test_sampling, 1076 checks, needs no model files. CLI: --temp/--top-k/
    --top-p/--seed; verified seed 1 twice == identical ids, seed 2 differs.
  - F023 streaming: generate() gained a TokenCallback fired per token before
    the next forward. StreamDecoder (tokenizer.*) holds back an incomplete
    trailing UTF-8 sequence until its continuation bytes arrive. test_streaming
    runs it over every committed corpus (English + unicode + specials + the HF
    greedy golden): each chunk must be structurally complete UTF-8 and the
    concatenation byte-identical to batch decode(). It also ASSERTS the
    hold-back path actually fired on the unicode corpus, so the test can't
    pass vacuously, and covers a 4-byte emoji arriving one byte-token at a
    time plus flush() of a stream cut mid-character. One-off: CLI generating
    Chinese output arrived in 25 separate read bursts (incremental, not
    buffered), decoded clean.
  - F024 allocation-free decode: layer_forward's per-call vectors moved into
    a Scratch struct owned by Engine; ensure() only grows, and the attention
    score row is sized to max_seq up front so it can't regrow mid-generation.
    test_alloc replaces GLOBAL operator new with a counter — with a canary
    check first, so a zero-count can't pass vacuously — and reports 0
    allocations across 16 greedy + 16 sampling decode steps, plus the F019
    golden reproducing exactly. Perf was UNCHANGED by this refactor alone
    (307 -> ~307 ms/token): the scalar matmul dominated. It is a prerequisite,
    not a win — the payoff is stable buffers for threading/SIMD.
  - F025 threading (src/core/threadpool.*): workers created once, parked on a
    condvar; a task is a function pointer + a ctx pointer to the caller's
    stack, so parallel_for allocates nothing (test_alloc still reports 0 with
    the pool live). linear() splits output columns, matmul() splits rows, so
    every element is computed by exactly one thread running the same serial
    kernel — test_ops asserts memcmp-identical output at 2/5/hardware threads
    on shapes whose sizes divide evenly by none of them. Small calls stay
    serial below a work threshold. CLI --threads N.
  - F026 SIMD: dot_f32 with four independent NEON accumulators (one
    accumulator would serialize on fmla latency); linear, the attention score
    loop, and rmsnorm all use it. rmsnorm keeps its DOUBLE sum-of-squares
    accumulator (via float64x2 lanes) — fp32 accumulation over 896 values
    drifts enough to threaten the golden. SIMD changes reduction order, so
    parity is by tolerance not memcmp: test_ops compares against an in-test
    scalar reference at lengths straddling the 16/4-wide unrolls, AND a
    second ctest target test_ops_scalar builds the same sources with
    NANO_FORCE_SCALAR so this arm64 machine actually executes the fallback
    path x86 CI will take, instead of assuming it compiles.
  - MEASURED THIS MACHINE (M3 Pro, 5 perf + 6 efficiency, 11 threads),
    bench/bench_ops.cpp medians of 20 reps. NOT bench-harness results — the
    README rule stands until F030, and nothing below is in the README.
      lm_head GEMV [151936x896], single thread: 84.4 ms scalar -> 8.34 ms
        NEON = 10.1x. Threaded on top: 4.82 ms.
      Threading speedup depends on regime, and this is the interesting part:
        compute-bound prefill GEMMs (tokens=41) get 4.35-4.68x, but
        decode GEMVs (tokens=1) only 1.7-2.5x — once SIMD removes the compute
        bottleneck, decode is memory-bandwidth bound (each weight is read once
        per token). F025's >=2.5x criterion was met against the scalar
        baseline it was written for (6.24x on lm_head then); post-SIMD the
        honest number for decode GEMV is ~1.7x, and prefill still clears 2.5x.
      End-to-end greedy, 41-token prompt, 32 new tokens:
        before session:      307 ms/token, TTFT ~10.0 s
        after F024:          ~307 ms/token, TTFT ~10.0 s (no change, expected)
        after F025:          64 ms/token,  TTFT 1.8 s
        after F026:          21.9 ms/token, TTFT 0.26 s   (14x / 38x overall)
      Side effect: ctest total 267 s -> 23.5 s (test_kvcache 183 s -> 9.5 s).
  - Every HF golden (layer, logits, greedy continuations, tokenizer) is
    unchanged through all five features — the SIMD reduction-order change
    shifted no token.
  - README Status list updated to reflect F013-F026 (feature status only, no
    numbers).
Next: F027 (int8 weight quantization — per-row scales, offline pass +
  quantized matmul; check dequant error, verify top-1 unchanged on the golden
  prompts, and measure fp32 vs int8 tok/s. Note decode is now bandwidth bound,
  so int8 should help decode MORE than its flop count suggests — that is the
  hypothesis to test). Then F028-F029 (pybind11 bindings + Python streaming),
  then F030 (the bench harness, which is what finally unlocks README numbers).
---
Session: 2026-08-18 (Session 2 — Forward pass)
Completed: F013-F021 (20/36 features passing; 8/8 ctest green)
Notes:
  - Venv gained torch 2.13 + transformers 5.15 (needed for HF-reference
    goldens; transformers 5.x note: rope params live in cfg.rope_parameters,
    apply_chat_template returns a BatchEncoding).
  - F013 chat template: C++ helper renders the no-tools branch of the
    tokenizer_config.json jinja (default/explicit system, multi-turn,
    generation prompt) then encode_with_specials. 5 golden cases vs HF
    apply_chat_template ids — exact.
  - F015 ops (src/core/ops.*): linear (y=xW^T+b, HF [out,in] layout), matmul,
    rmsnorm (double-accum mean), softmax, silu, add, mul vs numpy goldens at
    rtol 1e-5. F016 RoPE mirrors HF's float32 angle math (powf/cosf/sinf on
    float32 inv_freq) so q/k match transformers Qwen2RotaryEmbedding +
    apply_rotary_pos_emb at rtol 1e-5 up to position 1000. test_ops, 45
    checks, runs without model files.
  - F017-F018 (src/model/qwen2.*): Qwen2Model loads all weights bf16->fp32
    (~2GB, shape-checked); layer_forward = rmsnorm -> qkv+bias -> RoPE ->
    causal GQA (14q/2kv, group 7) -> o_proj -> residual -> rmsnorm -> SwiGLU
    -> residual; Engine = embed -> 24 layers -> final norm -> tied lm_head.
    KV cache built in from day one (prefill and decode share forward()).
    test_forward: embedding bit-exact vs HF layer-0 input; layer-0 out within
    4.5e-5 abs — MEASURED HF's own fp32 noise vs a float64 recompute at
    4.1e-5, so we sit at the noise floor (atol 1e-4 documented in the test);
    logits argmax exact on 3 prompts, top-10 + 512-point strided sample at
    1e-3.
  - F019 greedy: test_generate reproduces HF fp32 greedy continuations
    token-for-token on 4 prompts (3 stop at im_end, 1 full 32-token decode).
    CLI: nanoserve generate <dir> -p <prompt> -n N --greedy [--stats].
    GOLDEN-CAUGHT BUG: HF generate() applies generation_config.json's
    repetition_penalty=1.1 even with do_sample=False — first golden wasn't
    pure greedy and diverged on repeated tokens ("2+2": '+' got penalized).
    gen_golden.py now pins repetition_penalty=1.0 with a comment.
  - F014 Unicode: gen_unicode_tables.py generates unicode_tables.hpp
    (660 \p{L} + 137 \p{N} ranges, binary search). ALSO required NFC: the
    tokenizer.json declares an NFC normalizer — corpus caught decomposed
    accents diverging. unicode.cpp implements UAX#15 (pre-expanded canonical
    decompositions, ccc ordering, composition incl. algorithmic Hangul;
    invalid UTF-8 passes through via byte sentinels). Corpus +18 non-ASCII
    prompts (CJK/Cyrillic/Arabic/Devanagari/emoji ZWJ/NBSP/combining/math
    alphanumerics), all exact; goldens store HF's "decoded" as the roundtrip
    target since decode(encode(s)) == nfc(s). One-off fuzz: 300 random
    multi-script strings vs HF via the CLI — 300/300 exact.
  - F020 KV cache: test_kvcache proves cached decode logits are BIT-IDENTICAL
    to full recompute (5 steps), and measured 320 ms/token cached vs ~32
    s/token uncached at seq ~160 (printed by the test). Scalar fp32 baseline
    — perf is F024-F027.
  - F021 --stats: GenerateStats (ttft_ms + per-step ms) filled by
    greedy_generate; CLI prints TTFT, mean/median ms/token, tok/s. Two runs:
    8219/8169 ms TTFT, 310/320 ms/token mean (~3% apart, well under 20%).
  - Numbers above are from test/CLI output on this machine, NOT bench-harness
    results — do not put them in the README (rule stands until F030).
  - ctest total is now ~4.5 min (forward/generate/kvcache dominate); fine for
    init.sh, will shrink dramatically once F025/F026 land.
Next: F022 (sampling: temperature/top-k/top-p with seeded RNG, test_sampling
  with hand-built distributions + determinism checks), then F023 (streaming
  callback + UTF-8-safe incremental CLI printing), then F024 (allocation-free
  decode loop — scratch buffers currently live in layer_forward, move them
  into Engine; test_alloc with a debug allocation counter). fp32 remains the
  reference path for all of it.
---
Session: 2026-08-18 (Session 1 — Initializer)
Completed: Project scaffolding + F001-F003, F005-F012 (11/36 features passing)
Notes:
  - Initialized git repo, CMake C++20 project, warnings-as-errors
    (-Wall -Wextra -Wpedantic -Werror -Wshadow -Wconversion -Wsign-conversion).
    Zero third-party C++ deps by design.
  - Layout: src/core (json, dtype, mmap_file, safetensors), src/model (config,
    tokenizer), src/server (CLI), tests/ (plain-executable ctest tests,
    exit 77 = skip), bench/ (empty until F030), scripts/.
  - Model chosen: Qwen2.5-0.5B-Instruct — verified via HF API: license
    apache-2.0, gated=false, no token needed. Downloaded to
    models/qwen2.5-0.5b-instruct/ (942MB bf16 safetensors; git-ignored).
    Architecture confirmed: hidden 896, 24 layers, 14 heads / 2 kv heads (GQA),
    head_dim 64, vocab 151936, rope_theta 1e6, tied embeddings.
  - Implemented and tested:
    * Minimal strict JSON parser (objects/arrays/numbers/escapes incl. UTF-16
      surrogates; rejects malformed input) — test_json, 40 checks.
    * mmap'd safetensors loader: header parse, bounds validation, tensor lookup,
      bf16/f16 -> f32 widening. Verified against numpy goldens generated from
      the real checkpoint: 290 tensors, 494,032,768 params, embed values
      bit-exact — test_safetensors.
    * Typed ModelConfig from config.json with loud missing-field errors —
      test_config.
    * Byte-level BPE tokenizer (GPT-2 lineage): hand-rolled pretokenizer scanner
      mirroring Qwen's regex, merge-rank BPE, specials from
      tokenizer_config.json added_tokens_decoder. encode() matches HF
      `tokenizers` EXACTLY on a 25-prompt English corpus + 3 specials cases;
      decode() roundtrips byte-identically — test_tokenizer, 62 checks.
    * CLI: `nanoserve inspect` (tensor table + totals), `nanoserve tokenize`
      (ids + roundtrip check), `version`.
  - Bug found & fixed during testing: merges.txt loader treated the line
    "# $" as a comment; only the first "#version" line is a comment. Caught by
    the golden corpus (punctuation prompt), fixed, all 62 checks green.
  - Unicode note (honest limitation): pretokenizer \p{L}/\p{N} classes are
    exact for ASCII, approximate block ranges for non-ASCII. English corpus is
    exact-parity; full CJK/emoji parity is F014 (needs generated Unicode
    tables, llama.cpp-style).
  - Golden vectors are COMMITTED (tests/data/) so C++ tests never need Python.
    Regenerate with .venv/bin/python scripts/gen_golden.py (venv has
    tokenizers + numpy; .venv is git-ignored).
  - Verified CI skip path: with models/ absent, test_json passes and the three
    model-dependent tests report Skipped (exit 77 / SKIP_RETURN_CODE). CI
    workflow (.github/workflows/ci.yml, ubuntu+macos) is committed but F004
    stays false until the user creates the GitHub repo and pushes.
  - Per user direction: README/docs framed around inference-systems
    understanding (KV cache, quantization, serving, benchmarks), NOT C++
    showcase. Python surface (pybind11 bindings F028-F029, Python bench
    harness F030-F032) is in the feature list. Readability > cleverness.
  - init.sh green end-to-end: toolchain check, build, 4/4 ctest.
  - cmake was installed via homebrew this session (4.4.2). clang-format not
    installed; .clang-format committed, run it once installed.
Next: F013 (chat-template helper + golden ids via HF apply_chat_template —
  needed before generate() can build chat prompts), then F015 (fp32 ops:
  matmul/rmsnorm/softmax/silu vs numpy goldens — gen_golden.py needs an ops
  section) and F016 (RoPE). That unlocks the layer-forward chain F017-F019.
  Keep each op verified against a golden before wiring the next.
